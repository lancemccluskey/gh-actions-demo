name: tests-facts

on:
  pull_requests:
    paths:
      - 'pages/facts/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install && npm test -- -f facts/index.test
