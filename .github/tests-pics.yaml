name: tests-pics

on:
  pull_requests:
    paths:
      - 'pages/pics/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install && npm test -- -f pics/index.test
